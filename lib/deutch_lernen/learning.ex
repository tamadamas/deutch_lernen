defmodule DeutchLernen.Learning do
  @moduledoc """
  Learning domain implements user progress tracking and spaced repetition.

  This domain manages user accounts, word progress with the SM-2 algorithm,
  and study sessions for B1/B2 German certification preparation.
  """
  use Ash.Domain

  resources do
    # Resources will be registered here as they are created
  end
end
